# TeamHearing Database Schema

This document explains how the **musiclovers** database is structured.

---

## 1. Users table

The users table stores the user id (randomly generated), the username, email, password (hashed for security), birthdate, gender, role (which defaults to client), and volume settings.

## 2. Devices table

The devices table... idk. Cry cry cry